# TEST_SPEC.md — Named Test-Case Catalog (taskq-plus, Phase 2 / Round 3)

> **Phase 2 deliverable.** Generated by Agent A (ARCHITECT) following the
> `derive_test_cases.md` skill protocol. Test names from P1 TEST_INVENTORY.yaml
> are preserved verbatim. This is the SINGLE SOURCE OF TRUTH for all test
> traceability checks at Gates 1-4 (v2.6 unified D4). Validated by Agent B
> (TECH_LEAD) before P2 exit. P3 Agent A implements tests FROM this catalog,
> not ad-hoc.

**Project**: taskq-plus
**SRS version**: 01-requirements/SRS.md @ 2026-07-30 (v1.0.0)
**SAD version**: 02-architecture/SAD.md @ 2026-07-30 (v1.1.0)
**TEST_INVENTORY.yaml version**: TEST_INVENTORY.yaml @ 2026-07-30 (format_version 1.0)
**Generated**: 2026-07-30
**Active NFR Patterns**: NP-04, NP-06, NP-07, NP-08, NP-09, NP-10, NP-13, NP-15

---

## NFR Pattern Activation (Step 1 + Step 1b + Step 1c Output)

| Pattern | Trigger (SRS keyword / SAD module / SEC threat) | Activated |
|---------|--------------------------------------------------|-----------|
| NP-01 (auth 401)          | not applicable (CLI 單機本地,無 HTTP)              | ❌ |
| NP-02 (authz 403)         | not applicable (同上,無多租戶)                    | ❌ |
| NP-03 (rate limit 429)    | not applicable (無 HTTP endpoint)                 | ❌ |
| NP-04 (validation 422)    | SRS: "input validation"; FR-01 注入字元黑名單      | ✅ |
| NP-05 (idempotency)       | not found                                          | ❌ |
| NP-06 (latency SLA)       | SRS: "p95"; NFR-01 兩條性能預算                     | ✅ |
| NP-07 (dependency fault)  | SAD: service.cache / storage.cache_store (cache 層) | ✅ |
| NP-08 (security attack)   | SEC: T-01 / T-03 / T-05 (FR-01/FR-02/FR-07 攻擊面)  | ✅ |
| NP-09 (audit log)         | SRS: "結構化稽核日誌"; FR-08                         | ✅ |
| NP-10 (data round-trip)   | FR-04 cache lookup(put(x)) == x                    | ✅ |
| NP-11 (backward compat)   | not applicable (單輪交付,無 v1↔v2)                  | ❌ |
| NP-12 (pagination)        | not applicable (任務列表無分頁需求)                  | ❌ |
| NP-13 (concurrency)       | SAD: storage.task_store (Lock + threading + ThreadPoolExecutor) | ✅ |
| NP-14 (encryption)        | not applicable (無敏感欄位加密需求)                  | ❌ |
| NP-15 (timeout)           | SAD: service.executor (subprocess + timeout)        | ✅ |

Forced SAD integration cases: FR-04 cache fault-injection under `tests/integration/` (NP-07); FR-02 concurrent `run --all` writes under `tests/integration/` (NP-13); FR-03 subprocess-timeout wall-clock under `tests/integration/` (NP-15). SEC verified_by names (T-01..T-08) are listed under the FR they bind to and tagged SAD: in the test path.

---

## Functional Requirement Test Cases

### FR-01: 任務提交與驗證 (`submit "<cmd>" [--name] [--after]`)

**Classification**: API_ENDPOINT
**Active Patterns**: NP-04, NP-08, NP-13

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_fr01_submit_happy_path_emits_id` | command="echo hi"; expected_exit=0; state_mode="isolate_per_test"; subprocess_mode="in_process" | happy_path | Q1 |
| 2 | `test_fr01_submit_empty_command_rejected` | command=""; expected_exit=2; state_mode="isolate_per_test"; subprocess_mode="in_process" | validation | Q2 |
| 3 | `test_fr01_submit_injection_semicolon_rejected` | command="echo hi; rm x"; injection_char=";"; expected_exit=2; state_mode="isolate_per_test" | validation | Q2 + NP-04 |
| 4 | `test_fr01_submit_injection_pipe_rejected` | command="echo hi | cat"; injection_char="|"; expected_exit=2; state_mode="isolate_per_test" | validation | Q2 + NP-04 |
| 5 | `test_fr01_submit_injection_ampersand_rejected` | command="echo hi & echo y"; injection_char="&"; expected_exit=2; state_mode="isolate_per_test" | validation | Q2 + NP-04 |
| 6 | `test_fr01_submit_injection_dollar_rejected` | command="echo $HOME"; injection_char="$"; expected_exit=2; state_mode="isolate_per_test" | validation | Q2 + NP-04 |
| 7 | `test_fr01_submit_injection_gt_rejected` | command="echo hi > x"; injection_char=">"; expected_exit=2; state_mode="isolate_per_test" | validation | Q2 + NP-04 |
| 8 | `test_fr01_submit_injection_lt_rejected` | command="cat < x"; injection_char="<"; expected_exit=2; state_mode="isolate_per_test" | validation | Q2 + NP-04 |
| 9 | `test_fr01_submit_injection_backtick_rejected` | command="echo \`id\`"; injection_char="\`"; expected_exit=2; state_mode="isolate_per_test" | validation | Q2 + NP-04 |
| 10 | `test_fr01_submit_command_too_long_rejected` | command_len=1001; expected_exit=2; state_mode="isolate_per_test" | boundary | Q3 |
| 11 | `test_fr01_submit_whitespace_only_rejected` | command="   "; expected_exit=2; state_mode="isolate_per_test" | validation | Q2 |
| 12 | `test_fr01_submit_duplicate_name_rejected` | name="dup"; existing_name="dup"; expected_exit=2; state_mode="isolate_per_test" | validation | Q2 |
| 13 | `test_fr01_submit_invalid_dependency_rejected` | missing_dep="deadbeef"; expected_exit=2; state_mode="isolate_per_test" | validation | Q2 |
| 14 | `test_fr01_submit_emits_audit_event` | command="echo hi"; audit_event="submit"; state_mode="isolate_per_test"; subprocess_mode="out_of_process"; shared_TASKQ_HOME="false" | integration | Q1 + NP-09 |

**Sub-assertions** — predicate over a case's declared Inputs / the production `result`. Predicates are Python-expression syntax (e.g. `len(result) == 4`) so the P2 self-consistency engine (`harness_cli.py check-test-spec-consistency`) can evaluate them.

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| AC1-id-len-8 | `len(result.task_id_str) == 8` | 1 |
| AC1-exit-0 | `result.exit_code == 0` | 1 |
| AC1-injection-char-present | `injection_char in command` | 3, 4, 5, 6, 7, 8, 9 |
| AC1-injection-exit-2 | `result.exit_code == 2` | 3, 4, 5, 6, 7, 8, 9 |
| AC1-empty-rejected | `result.exit_code == 2` | 2, 11 |
| AC1-whitespace-only | `command.strip() == ""` | 11 |
| AC1-command-too-long | `command_len > 1000` | 10 |
| AC1-too-long-exit-2 | `result.exit_code == 2` | 10 |
| AC1-dup-name-clash | `name == existing_name` | 12 |
| AC1-dup-name-exit-2 | `result.exit_code == 2` | 12 |
| AC1-missing-dep | `missing_dep not in known_task_ids` | 13 |
| AC1-missing-dep-exit-2 | `result.exit_code == 2` | 13 |
| AC1-audit-event-name | `result.audit_event == "submit"` | 14 |
| AC1-audit-task-id-matches | `result.audit_task_id == result.task_id_str` | 14 |

---

### FR-02: 任務執行器 (`run <id>` / `run --all`)

**Classification**: INTEGRATION
**Active Patterns**: NP-13, NP-15

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_fr02_shell_false_invariant` | path_str="03-development/src/"; pattern="shell=True"; expected_hits=0; subprocess_mode="in_process" | security_grep | Q6 + NP-08 |
| 2 | `test_fr02_completion_fields_atomic_write` | command="true"; expected_fields="exit_code,stdout_tail,stderr_tail,duration_ms,finished_at"; state_mode="isolate_per_test"; subprocess_mode="in_process" | happy_path | Q1 + NFR-03 |
| 3 | `test_fr02_run_all_kahn_topological_order` | tasks_n=8; max_workers=4; edges="[(0,1),(1,2)]"; state_mode="isolate_per_test"; subprocess_mode="out_of_process"; shared_TASKQ_HOME="false" | integration | Q7 + NP-13 |
| 4 | `test_fr02_timeout_yields_exit_4` | sleep_seconds=5; task_timeout=1; expected_exit=4; state_mode="isolate_per_test"; subprocess_mode="out_of_process"; shared_TASKQ_HOME="false" | timeout | Q5 + NP-15 |
| 5 | `test_fr02_stdout_tail_truncated_2000_chars` | command="printf 'a%.0s' {1..3000}"; tail_max=2000; state_mode="isolate_per_test"; subprocess_mode="in_process" | boundary | Q3 |

**Sub-assertions**

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| AC2-shell-true-hits-zero | `result.hit_count == 0` | 1 |
| AC2-fields-present | `expected_fields == "exit_code,stdout_tail,stderr_tail,duration_ms,finished_at"` | 2 |
| AC2-atomic-single-write | `result.transition_writes == 1` | 2 |
| AC2-run-all-concurrency | `max_workers == 4` | 3 |
| AC2-kahn-order-preserves-deps | `all(topo_pos[u] < topo_pos[v] for u, v in edges)` | 3 |
| AC2-timeout-status | `result.task_status == "timeout"` | 4 |
| AC2-timeout-exit-4 | `result.exit_code == 4` | 4 |
| AC2-task-timeout-elapsed-lt-budget | `result.elapsed_seconds < task_timeout + 2` | 4 |
| AC2-tail-len-cap | `len(result.stdout_tail) <= tail_max` | 5 |
| AC2-tail-is-suffix | `result.stdout_tail == raw_output[-2000:]` | 5 |

**Properties**

| property_id | invariant (predicate over Inputs / result) | applies_to (case #) |
|---|---|---|
| P2-shell-invariant-globally | `grep_shell_true_count(path_str) == 0` | 1 |

> The property holds regardless of the specific `pattern` value (always `shell=True` here). The "global" framing documents that no `subprocess.run(..., shell=True, ...)` call exists anywhere in the codebase — this is NFR-02's binding constraint, not a per-test behaviour.

---

### FR-03: 重試與斷路器

**Classification**: STATE_MACHINE
**Active Patterns**: NP-13, NP-15

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_fr03_retry_with_exponential_backoff` | retry_limit=2; backoff_base=0.1; sleep_inject="fake"; state_mode="isolate_per_test"; subprocess_mode="in_process" | happy_path | Q4 |
| 2 | `test_fr03_breaker_threshold_persists_open` | threshold=3; final_failures=3; expected_state="OPEN"; state_mode="isolate_per_test"; subprocess_mode="in_process" | state_transition | Q4 + Q5 |
| 3 | `test_fr03_breaker_open_rejects_with_exit_3` | initial_state="OPEN"; expected_exit=3; state_mode="isolate_per_test"; subprocess_mode="in_process" | validation | Q4 |
| 4 | `test_fr03_breaker_half_open_success_closes` | prior_state="HALF_OPEN"; admit_outcome="success"; expected_state="CLOSED"; counter_expected=0; state_mode="isolate_per_test"; subprocess_mode="in_process" | state_transition | Q4 |
| 5 | `test_fr03_breaker_half_open_failure_reopens` | prior_state="HALF_OPEN"; admit_outcome="failure"; expected_state="OPEN"; state_mode="isolate_per_test"; subprocess_mode="in_process" | state_transition | Q4 |
| 6 | `test_fr03_breaker_recovers_after_cooldown` | threshold=3; cooldown_seconds=5; prior_state="OPEN"; state_mode="isolate_per_test"; subprocess_mode="out_of_process"; shared_TASKQ_HOME="false" | integration | Q7 + NP-15 |

**Sub-assertions**

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| AC3-retry-count-le-limit | `result.attempt_count <= retry_limit` | 1 |
| AC3-backoff-exponential | `result.sleep_calls == [backoff_base, backoff_base * 2]` | 1 |
| AC3-sleep-injectable | `sleep_inject == "fake"` | 1 |
| AC3-threshold-persisted-open | `result.breaker_state == "OPEN"` | 2 |
| AC3-atomic-persist | `result.persist_writes == 1` | 2 |
| AC3-open-exit-3 | `result.exit_code == 3` | 3 |
| AC3-no-subprocess-on-open | `result.subprocess_spawned == 0` | 3 |
| AC3-half-open-success-closes | `result.breaker_state == "CLOSED"` | 4 |
| AC3-half-open-counter-reset | `result.failure_count == counter_expected` | 4 |
| AC3-half-open-failure-reopens | `result.breaker_state == "OPEN"` | 5 |
| AC3-cooldown-elapsed | `result.elapsed_seconds >= cooldown_seconds` | 6 |
| AC3-recovery-exit-0 | `result.exit_code == 0` | 6 |

**Properties**

| property_id | invariant (predicate over Inputs / result) | applies_to (case #) |
|---|---|---|
| P3-monotone-failure-count | `record_failure(c) == c + 1` | 2 |
| P3-record-success-noop-on-closed | `record_success(CLOSED, c) == c` | 4 |

> P3 captures the algebraic invariant of the breaker counter: `record_failure` is strictly monotonic, `record_success` only resets to zero (never decrements below). The CLOSED→OPEN→HALF_OPEN→CLOSED/OPEN state transitions are checked by the state_transition cases above; these properties cover the arithmetic that backstops them.

---

### FR-04: 結果 TTL 快取

**Classification**: DATA_ENTITY
**Active Patterns**: NP-07, NP-10, NP-13

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_fr04_cache_signature_is_sha256_of_command` | command="echo hi"; expected_sig="56a79f3b115448072387c2480044bfa2cf8f90e4f5fddd8c943b4e051b81f80b"; state_mode="isolate_per_test"; subprocess_mode="in_process" | happy_path | Q1 |
| 2 | `test_fr04_cache_hit_replays_without_subprocess` | cached_at_offset=10; ttl_seconds=3600; expected_cached_flag="true"; state_mode="isolate_per_test"; subprocess_mode="in_process" | happy_path | Q1 + NP-10 |
| 3 | `test_fr04_cache_miss_atomic_write` | cache_present=False; command="echo hi"; state_mode="isolate_per_test"; subprocess_mode="in_process" | boundary | Q3 + NFR-03 |
| 4 | `test_fr04_cache_thread_safe_with_run_all` | tasks_n=20; max_workers=4; state_mode="isolate_per_test"; subprocess_mode="out_of_process"; shared_TASKQ_HOME="false" | integration | Q7 + NP-13 |

**Sub-assertions**

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| AC4-sig-matches-sha256 | `result.cache_key_str == expected_sig` | 1 |
| AC4-sig-deterministic | `result.cache_key_str == sha256_hex(command)` | 1 |
| AC4-cached-flag-set | `expected_cached_flag == "true"` | 2 |
| AC4-no-subprocess-on-hit | `result.subprocess_spawned == 0` | 2 |
| AC4-replayed-exit-code | `result.replayed_exit_code == result.stored_exit_code` | 2 |
| AC4-miss-triggers-run | `result.subprocess_spawned == 1` | 3 |
| AC4-miss-atomic-write | `result.persist_writes == 1` | 3 |
| AC4-thread-safe-final-state | `result.cache_json_parse_ok == True` | 4 |

**Properties**

| property_id | invariant (predicate over Inputs / result) | applies_to (case #) |
|---|---|---|
| P4-sig-idempotent | `cache_key(command) == cache_key(command)` | 1 |
| P4-cache-roundtrip | `cache_lookup(cache_store(sig, result_str)) == result_str` | 2, 3 |

> P4-sig-idempotent: same input always maps to the same signature — the cache key is a pure function of the command string. P4-cache-roundtrip: writing then reading returns the original payload — the round-trip holds for any `sig → result` pair. Both must be killed by any mutation that breaks the cache layer (mutmut strength).

---

### FR-05: CLI 整合 (click group + 8 子命令 + exit-code map)

**Classification**: API_ENDPOINT
**Active Patterns**: (no active pattern; FR-05 is the wiring surface, not a new failure mode)

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_fr05_cli_reachable_submit` | subcommand="submit"; state_mode="isolate_per_test"; subprocess_mode="out_of_process"; shared_TASKQ_HOME="false" | happy_path | Q1 |
| 2 | `test_fr05_cli_reachable_run` | subcommand="run"; state_mode="isolate_per_test"; subprocess_mode="out_of_process"; shared_TASKQ_HOME="false" | happy_path | Q1 |
| 3 | `test_fr05_cli_reachable_status` | subcommand="status"; state_mode="isolate_per_test"; subprocess_mode="out_of_process"; shared_TASKQ_HOME="false" | happy_path | Q1 |
| 4 | `test_fr05_cli_reachable_list` | subcommand="list"; state_mode="isolate_per_test"; subprocess_mode="out_of_process"; shared_TASKQ_HOME="false" | happy_path | Q1 |
| 5 | `test_fr05_cli_reachable_graph` | subcommand="graph"; state_mode="isolate_per_test"; subprocess_mode="out_of_process"; shared_TASKQ_HOME="false" | happy_path | Q1 |
| 6 | `test_fr05_cli_reachable_plugins_list` | subcommand="plugins list"; state_mode="isolate_per_test"; subprocess_mode="out_of_process"; shared_TASKQ_HOME="false" | happy_path | Q1 |
| 7 | `test_fr05_cli_reachable_export` | subcommand="export"; state_mode="isolate_per_test"; subprocess_mode="out_of_process"; shared_TASKQ_HOME="false" | happy_path | Q1 |
| 8 | `test_fr05_cli_reachable_clear` | subcommand="clear"; state_mode="isolate_per_test"; subprocess_mode="out_of_process"; shared_TASKQ_HOME="false" | happy_path | Q1 |
| 9 | `test_fr05_cli_json_flag_produces_single_line_json` | subcommand="status"; expected_newlines=1; state_mode="isolate_per_test"; subprocess_mode="out_of_process"; shared_TASKQ_HOME="false" | validation | Q2 |
| 10 | `test_fr05_exit_code_0_success` | scenario="happy_submit"; expected_exit=0; state_mode="isolate_per_test"; subprocess_mode="out_of_process"; shared_TASKQ_HOME="false" | state_transition | Q4 |
| 11 | `test_fr05_exit_code_2_validation_error` | scenario="submit_empty"; expected_exit=2; state_mode="isolate_per_test"; subprocess_mode="out_of_process"; shared_TASKQ_HOME="false" | state_transition | Q4 |
| 12 | `test_fr05_exit_code_3_breaker_open` | scenario="run_while_open"; expected_exit=3; state_mode="isolate_per_test"; subprocess_mode="out_of_process"; shared_TASKQ_HOME="false" | state_transition | Q4 |
| 13 | `test_fr05_exit_code_4_timeout` | scenario="run_sleep_5_with_timeout_1"; expected_exit=4; state_mode="isolate_per_test"; subprocess_mode="out_of_process"; shared_TASKQ_HOME="false" | state_transition | Q4 |
| 14 | `test_fr05_exit_code_5_dependency_cycle` | scenario="cycle_a_b_a"; expected_exit=5; state_mode="isolate_per_test"; subprocess_mode="out_of_process"; shared_TASKQ_HOME="false" | state_transition | Q4 |
| 15 | `test_fr05_exit_code_6_plugin_load_failure` | scenario="plugins_path_rejected"; expected_exit=6; state_mode="isolate_per_test"; subprocess_mode="out_of_process"; shared_TASKQ_HOME="false" | state_transition | Q4 |
| 16 | `test_fr05_exit_code_1_internal_error` | scenario="corrupt_tasks_json"; expected_exit=1; state_mode="isolate_per_test"; subprocess_mode="out_of_process"; shared_TASKQ_HOME="false" | state_transition | Q4 |

**Sub-assertions**

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| AC5-subcommand-reachable | `result.subcommand_in_help == True` | 1, 2, 3, 4, 5, 6, 7, 8 |
| AC5-json-single-line | `result.stdout_newlines == expected_newlines` | 9 |
| AC5-exit-0-success | `result.exit_code == 0` | 10 |
| AC5-exit-2-validation | `result.exit_code == 2` | 11 |
| AC5-exit-3-breaker-open | `result.exit_code == 3` | 12 |
| AC5-exit-4-timeout | `result.exit_code == 4` | 13 |
| AC5-exit-5-cycle | `result.exit_code == 5` | 14 |
| AC5-exit-6-plugin | `result.exit_code == 6` | 15 |
| AC5-exit-1-internal | `result.exit_code == 1` | 16 |
| AC5-corrupt-store-detected | `result.detected_corruption == True` | 16 |

---

### FR-06: 任務相依 DAG (Kahn 拓撲 + 循環偵測 + 深度上限)

**Classification**: ALGORITHM
**Active Patterns**: NP-13

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_fr06_dag_kahn_order_happy_path` | nodes="a,b"; edge="a->b"; prior_a_status="done"; expected_b_status="done"; precondition="submit a then submit b --after a; run a to done; run --all; b executes after a"; state_mode="isolate_per_test"; subprocess_mode="out_of_process"; shared_TASKQ_HOME="false" | integration | Q7 + NP-13 |
| 2 | `test_fr06_dag_depends_on_persisted_to_tasks_json` | edges="a->b"; state_mode="isolate_per_test"; subprocess_mode="in_process" | happy_path | Q1 |
| 3 | `test_fr06_dag_cycle_rejected_with_path` | nodes="a,b,c"; edges="a->b,b->c,c->a"; expected_exit=5; state_mode="isolate_per_test"; subprocess_mode="in_process" | validation | Q2 |
| 4 | `test_fr06_dag_depth_cap_rejected` | chain_len=33; max_depth=32; expected_exit=5; state_mode="isolate_per_test"; subprocess_mode="in_process" | boundary | Q3 |
| 5 | `test_fr06_graph_text_outputs_indented_tree` | format_choice="text"; state_mode="isolate_per_test"; subprocess_mode="out_of_process"; shared_TASKQ_HOME="false" | happy_path | Q1 |
| 6 | `test_fr06_graph_dot_outputs_graphviz_dot` | format_choice="dot"; expected_header="digraph"; state_mode="isolate_per_test"; subprocess_mode="out_of_process"; shared_TASKQ_HOME="false" | happy_path | Q1 |
| 7 | `test_fr06_dag_blocked_does_not_count_for_breaker` | upstream_status="failed"; downstream_expected="blocked"; breaker_count_expected=0; state_mode="isolate_per_test"; subprocess_mode="in_process" | state_transition | Q4 |

**Sub-assertions**

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| AC6-kahn-order | `result.topo_pos[a] < result.topo_pos[b]` | 1 |
| AC6-blocked-not-done | `result.b_status == "blocked"` | 7 |
| AC6-blocked-doesnt-count | `result.breaker_failure_count == breaker_count_expected` | 7 |
| AC6-depends-on-persisted | `result.tasks_json["b"]["depends_on"] == ["a"]` | 2 |
| AC6-cycle-detected | `result.exit_code == 5` | 3 |
| AC6-cycle-path-in-stderr | `"->" in result.stderr_cycle_path` | 3 |
| AC6-depth-exit-5 | `result.exit_code == 5` | 4 |
| AC6-depth-message | `"dependency chain too deep" in result.stderr_text` | 4 |
| AC6-text-format-output | `result.stdout_first_char == " "` | 5 |
| AC6-dot-format-output | `result.stdout_first_chars == expected_header` | 6 |

**Properties**

| property_id | invariant (predicate over Inputs / result) | applies_to (case #) |
|---|---|---|
| P6-topo-order-respects-deps | `all(topo_pos[u] < topo_pos[v] for u, v in edges)` | 1, 2 |

> P6 captures the contract of Kahn's algorithm: for every edge (u→v) in the input, u appears before v in the produced ordering. This invariant must hold for ANY DAG passed in — not just the declared test cases — and is the algorithm's correctness condition.

---

### FR-07: Plugin Hook 系統 (allowlist + import_module + 例外隔離)

**Classification**: SECURITY_CONTROL
**Active Patterns**: NP-08

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_fr07_plugin_path_form_rejected` | plugin_spec="../evil.py"; expected_exit=6; state_mode="isolate_per_test"; subprocess_mode="out_of_process"; shared_TASKQ_HOME="false" | validation | Q2 + NP-08 |
| 2 | `test_fr07_plugin_invalid_regex_rejected` | plugin_spec="1bad-name"; expected_exit=6; state_mode="isolate_per_test"; subprocess_mode="out_of_process"; shared_TASKQ_HOME="false" | validation | Q2 |
| 3 | `test_fr07_plugin_error_does_not_abort_run` | plugin_raises_in="pre_run"; expected_task_status="done"; state_mode="isolate_per_test"; subprocess_mode="out_of_process"; shared_TASKQ_HOME="false" | integration | Q7 |
| 4 | `test_fr07_plugin_3_consecutive_failures_disables` | failure_streak=3; state_mode="isolate_per_test"; subprocess_mode="in_process" | state_transition | Q4 |
| 5 | `test_fr07_plugin_eval_exec_import_disabled` | path_str="03-development/src/"; pattern="eval(\|exec(\|__import__("; expected_hits=0; subprocess_mode="in_process" | security_grep | Q6 + NP-08 |

**Sub-assertions**

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| AC7-path-form-exit-6 | `result.exit_code == 6` | 1 |
| AC7-invalid-regex-exit-6 | `result.exit_code == 6` | 2 |
| AC7-error-not-aborting | `result.task_status == "done"` | 3 |
| AC7-plugin-error-audit-event | `result.audit_event_present == True` | 3 |
| AC7-3-failures-disabled | `result.plugin_disabled == True` | 4 |
| AC7-eval-exec-grep-zero | `result.hit_count == 0` | 5 |

---

### FR-08: 結構化稽核日誌與匯出

**Classification**: DATA_ENTITY
**Active Patterns**: NP-09

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_fr08_audit_jsonl_append_with_fsync` | audit_path="$TASKQ_HOME/audit.jsonl"; expected_mode="append"; invocation_events=1; state_mode="isolate_per_test"; subprocess_mode="in_process" | happy_path | Q1 + NP-09 |
| 2 | `test_fr08_audit_correlation_id_shared_per_invocation` | invocation_events=3; state_mode="isolate_per_test"; subprocess_mode="out_of_process"; shared_TASKQ_HOME="false" | integration | Q7 + NP-09 |
| 3 | `test_fr08_secret_in_command_not_in_audit_jsonl` | secret_in_stdout="sk-abcdefgh1234"; audit_path="$TASKQ_HOME/audit.jsonl"; expected_grep_count=0; state_mode="isolate_per_test"; subprocess_mode="out_of_process"; shared_TASKQ_HOME="false" | integration | Q7 + NP-09 |
| 4 | `test_fr08_export_json_format` | format_choice="json"; expected_envelope="array"; state_mode="isolate_per_test"; subprocess_mode="out_of_process"; shared_TASKQ_HOME="false" | happy_path | Q1 |
| 5 | `test_fr08_export_csv_format` | format_choice="csv"; expected_header_present=True; state_mode="isolate_per_test"; subprocess_mode="out_of_process"; shared_TASKQ_HOME="false" | happy_path | Q1 |
| 6 | `test_fr08_export_md_format` | format_choice="md"; expected_pipe_count_per_row=4; state_mode="isolate_per_test"; subprocess_mode="out_of_process"; shared_TASKQ_HOME="false" | happy_path | Q1 |
| 7 | `test_fr08_export_csv_rfc4180_quoting` | field_value="a,b\"c"; state_mode="isolate_per_test"; subprocess_mode="in_process" | boundary | Q3 |

**Sub-assertions**

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| AC8-jsonl-one-event-per-line | `result.audit_lines_count == invocation_events` | 1 |
| AC8-fsync-per-write | `result.fsync_call_count == result.append_call_count` | 1 |
| AC8-correlation-id-equal-across-events | `len(set(result.correlation_ids)) == 1` | 2 |
| AC8-secret-absent-from-audit | `result.grep_sk_count == 0` | 3 |
| AC8-json-array-envelope | `result.json_root_is_list == True` | 4 |
| AC8-csv-header-present | `result.csv_first_row_is_header == expected_header_present` | 5 |
| AC8-md-pipe-count | `result.md_pipes_per_row == expected_pipe_count_per_row` | 6 |
| AC8-csv-rfc4180-quoted-field | `result.csv_field_value_quoted == True` | 7 |
| AC8-row-counts-equal | `result.json_row_count == result.csv_row_count == result.md_row_count` | 4, 5, 6 |

**Properties**

| property_id | invariant (predicate over Inputs / result) | applies_to (case #) |
|---|---|---|
| P8-correlation-id-constant-per-invocation | `event_a["correlation_id"] == event_b["correlation_id"]` | 2 |

> P8 captures the audit-log's structural invariant: every JSONL line produced by a single CLI invocation shares one correlation_id, regardless of how many events fire. The predicate is symbolic — a and b range over the events of one invocation — and the declared integration case #2 enumerates the smallest non-trivial scenario (3 events, 1 invocation).

---

## Non-Functional Requirement Test Cases

> Per NFR Layering protocol: integration-level NFRs (perf benchmarks, integration coverage, fault injection, end-to-end redaction) carry full Inputs + Sub-assertions tables. Unit/Static NFRs (grep / bandit / lint / license / mutation / readability / verify-system) are deferred to a separate `Deferred to Downstream Phases` table below because their verifier is an external tool, not a TEST_SPEC in-process predicate.

### NFR-01: 效能預算 (perf benchmark)

**Classification**: INFRASTRUCTURE
**Active Patterns**: NP-06

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_perf_submit_status_p95_below_50ms` | iterations=100; expected_p95_ms_lt=50; state_mode="isolate_per_test"; subprocess_mode="in_process" | performance | Q1 + NP-06 |
| 2 | `test_perf_run_all_toposort_p95_below_200ms` | tasks_n=200; expected_p95_ms_lt=200; state_mode="isolate_per_test"; subprocess_mode="in_process" | performance | Q1 + NP-06 |

**Sub-assertions**

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| NFR01-iterations-count | `result.iterations_run == iterations` | 1 |
| NFR01-p95-under-budget | `result.p95_ms < expected_p95_ms_lt` | 1 |
| NFR01-submit-status-no-subprocess | `result.subprocess_spawned_total == 0` | 1 |
| NFR01-toposort-iterations-count | `result.tasks_processed == tasks_n` | 2 |
| NFR01-toposort-p95-under-budget | `result.p95_ms < expected_p95_ms_lt` | 2 |

---

### NFR-02: 執行與載入安全 (security gates)

**Classification**: SECURITY_CONTROL
**Active Patterns**: NP-08

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_security_grep_shell_true_returns_zero` | path_str="03-development/src/"; pattern="shell=True"; expected_hits=0; subprocess_mode="in_process" | security_grep | Q6 + NP-08 |
| 2 | `test_security_per_char_injection_seven_chars` | chars_list=";\|&$><`"; expected_cases="TC-FR01-03a..g"; expected_set_chars=";\|&$><`"; subprocess_mode="in_process" | meta_test | Q6 + NP-04 + NP-08 |
| 3 | `test_security_grep_eval_exec_import_returns_zero` | path_str="03-development/src/"; pattern="eval(\|exec(\|__import__( "; expected_hits=0; subprocess_mode="in_process" | security_grep | Q6 + NP-08 |
| 4 | `test_security_plugin_allowlist_regex` | plugin_spec="../evil.py"; expected_exit=6; state_mode="isolate_per_test"; subprocess_mode="out_of_process"; shared_TASKQ_HOME="false" | security_gate | Q2 + NP-08 |
| 5 | `test_security_bandit_zero_high_medium` | path_str="03-development/src/"; expected_high=0; expected_medium=0; subprocess_mode="in_process" | security_grep | Q6 + NP-08 |

**Sub-assertions**

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| NFR02-shell-true-hits-zero | `result.hit_count == expected_hits` | 1 |
| NFR02-seven-chars-covered | `len(chars_list) == 7` | 2 |
| NFR02-chars-set-equal-allowlist | `sorted(chars_list) == sorted(expected_set_chars)` | 2 |
| NFR02-eval-exec-hits-zero | `result.hit_count == expected_hits` | 3 |
| NFR02-plugin-path-rejected | `result.exit_code == expected_exit` | 4 |
| NFR02-bandit-high-zero | `result.bandit_high == expected_high` | 5 |
| NFR02-bandit-medium-zero | `result.bandit_medium == expected_medium` | 5 |

---

### NFR-03: 錯誤處理與原子性 (storage.atomic + breaker recovery timing)

**Classification**: INFRASTRUCTURE
**Active Patterns**: (deferred for the static-ast parts)

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_error_tasks_json_atomic_write` | target_file="tasks.json"; mid_write_signal="SIGKILL"; state_mode="isolate_per_test"; subprocess_mode="in_process" | integration | Q5 |
| 2 | `test_error_breaker_json_atomic_write` | target_file="breaker.json"; mid_write_signal="SIGKILL"; state_mode="isolate_per_test"; subprocess_mode="in_process" | integration | Q5 |
| 3 | `test_error_cache_json_atomic_write` | target_file="cache.json"; mid_write_signal="SIGKILL"; state_mode="isolate_per_test"; subprocess_mode="in_process" | integration | Q5 |
| 4 | `test_error_audit_jsonl_append_fsync` | target_file="audit.jsonl"; mid_write_signal="SIGKILL"; state_mode="isolate_per_test"; subprocess_mode="in_process" | integration | Q5 |
| 5 | `test_error_no_bare_except_clause` | path_str="03-development/src/"; pattern="^except:$"; expected_hits=0; subprocess_mode="in_process" | static_check | Q6 |
| 6 | `test_error_breaker_open_to_closed_within_cooldown_plus_1s` | cooldown_seconds=5; max_recovery_seconds=6; state_mode="isolate_per_test"; subprocess_mode="in_process" | timing | Q4 + NP-15 |

**Sub-assertions**

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| NFR03-tasks-json-parseable | `result.json_parse_ok == True` | 1 |
| NFR03-breaker-json-parseable | `result.json_parse_ok == True` | 2 |
| NFR03-cache-json-parseable | `result.json_parse_ok == True` | 3 |
| NFR03-audit-no-half-line | `result.last_line_complete == True` | 4 |
| NFR03-no-bare-except | `result.hit_count == expected_hits` | 5 |
| NFR03-recovery-under-budget | `result.recovery_seconds <= max_recovery_seconds` | 6 |
| NFR03-recovery-gte-cooldown | `result.recovery_seconds >= cooldown_seconds` | 6 |

---

### NFR-04: 敏感資料遮蔽 (write-time redaction across 3 output channels)

**Classification**: SECURITY_CONTROL
**Active Patterns**: (NP-08 indirectly; redactor is the security control itself)

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_redact_stdout_tail_applied_before_write` | secret="sk-abcdefgh1234"; channel="stdout_tail"; state_mode="isolate_per_test"; subprocess_mode="in_process" | integration | Q5 + Q7 |
| 2 | `test_redact_stderr_tail_applied_before_write` | secret="Bearer eyJhbGciOi"; channel="stderr_tail"; state_mode="isolate_per_test"; subprocess_mode="in_process" | integration | Q5 + Q7 |
| 3 | `test_redact_audit_detail_applied_before_write` | secret="token=s3cret-value"; channel="audit_detail"; state_mode="isolate_per_test"; subprocess_mode="in_process" | integration | Q5 + Q7 |
| 4 | `test_redact_sk_secret_in_audit_jsonl_grep_zero` | audit_path="$TASKQ_AUDIT_LOG"; secret="sk-abcdefgh1234"; expected_grep_count=0; state_mode="isolate_per_test"; subprocess_mode="out_of_process"; shared_TASKQ_HOME="false" | integration | Q7 |
| 5 | `test_redact_in_memory_not_persisted` | secret="sk-abcdefgh1234"; persisted_targets="audit.jsonl,tasks.json,cache.json"; state_mode="isolate_per_test"; subprocess_mode="out_of_process"; shared_TASKQ_HOME="false" | integration | Q7 |

**Sub-assertions**

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| NFR04-redact-replaced | `result.redacted_substring_count == 1` | 1, 2, 3 |
| NFR04-redact-marker-present | `result.redacted_marker == "[REDACTED]"` | 1, 2, 3 |
| NFR04-sk-grep-zero | `result.grep_sk_count == expected_grep_count` | 4 |
| NFR04-no-plaintext-on-disk | `result.plaintext_secret_count == 0` | 5 |

---

### NFR-10: 整合覆蓋 (CLI-driven integration suite, ≥ 80% line)

**Classification**: INTEGRATION
**Active Patterns**: NP-13

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_int_cov_line_at_least_80_percent` | integration_root="03-development/tests/integration"; expected_min_pct=80; subprocess_mode="out_of_process" | coverage_gate | Q6 |
| 2 | `test_int_driven_via_cli_or_clirunner` | invocations="submit,run,status,graph,export,clear"; subprocess_mode="out_of_process" | coverage_gate | Q6 |
| 3 | `test_int_spans_submit_to_run_to_status_chain` | scenarios="submit_then_run_then_status, dag_multi_layer, breaker_open_close, cache_hit, plugin_hook, export_three_formats"; state_mode="isolate_per_test"; subprocess_mode="out_of_process"; shared_TASKQ_HOME="false" | coverage_gate | Q7 |

**Sub-assertions**

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| NFR10-cov-pct | `result.coverage_pct >= expected_min_pct` | 1 |
| NFR10-cov-all-clis | `result.cli_invocations_covered == len(invocations)` | 2 |
| NFR10-chain-submit-run-status | `result.scenario_results["submit_then_run_then_status"] == "pass"` | 3 |
| NFR10-chain-dag | `result.scenario_results["dag_multi_layer"] == "pass"` | 3 |
| NFR10-chain-breaker | `result.scenario_results["breaker_open_close"] == "pass"` | 3 |
| NFR10-chain-cache | `result.scenario_results["cache_hit"] == "pass"` | 3 |
| NFR10-chain-plugin | `result.scenario_results["plugin_hook"] == "pass"` | 3 |
| NFR10-chain-export | `result.scenario_results["export_three_formats"] == "pass"` | 3 |

---

### Deferred to Downstream Phases

> Per the NFR Layering protocol, the following NFRs are verified by external static/lint tools, NOT by an in-process predicate that TEST_SPEC.md can self-consistency-check. They are listed here so the P3 implementer knows the binding test function name and that the gate lives at the harness level (run by `make verify-system` or its sub-targets), not in the pytest collection driven by the Cases tables above.

| # | NFR | Test Function | Layer | Title |
|---|---|---|---|---|
| 1 | NFR-05 | `test_docs_ast_docstrings_100_percent` | docs | ast-docstrings 100% public coverage |
| 2 | NFR-05 | `test_docs_docstring_contains_fr_or_nfr_tag` | docs | every docstring carries [FR-XX] or [NFR-XX] tag |
| 3 | NFR-06 | `test_arch_importlinter_exists_and_declares_layers` | arch | .importlinter existence + layers contract |
| 4 | NFR-06 | `test_arch_lint_imports_exit_zero` | arch | lint-imports exit 0 |
| 5 | NFR-06 | `test_arch_no_blanket_ignore_imports` | arch | no weakening via blanket ignore / single forbidden rule |
| 6 | NFR-07 | `test_license_requirements_txt_pin_eq` | license | every runtime dep pinned with == |
| 7 | NFR-07 | `test_license_pip_licenses_allowlist` | license | every dep license in allowlist |
| 8 | NFR-07 | `test_license_sbom_json_present` | license | 08-config/SBOM.json exists with name/version/license |
| 9 | NFR-08 | `test_mutation_harness_config_flag_true` | mutation | harness_config mutation_testing = true |
| 10 | NFR-08 | `test_mutation_score_at_least_70` | mutation | mutmut score ≥ 70 over service/ + storage/ |
| 11 | NFR-09 | `test_zero_skip_pytest_skipped_zero` | zero_skip | pytest summary skipped == 0 |
| 12 | NFR-09 | `test_zero_skip_ast_assertions_zero` | zero_skip | zero-assert test functions == 0 |
| 13 | NFR-09 | `test_zero_skip_no_ignore_k_deselect_collect_ignore` | zero_skip | no exclusion mechanism used |
| 14 | NFR-09 | `test_zero_skip_traceability_verified_only_after_run` | zero_skip | VERIFIED only post-actual-run |
| 15 | NFR-11 | `test_readability_mi_at_least_80` | readability | project MI ≥ 80 |
| 16 | NFR-11 | `test_readability_cyclomatic_complexity_le_10` | readability | cyclomatic ≤ 10/function |
| 17 | NFR-11 | `test_readability_file_loc_le_400_dir_files_le_15` | readability | ≤ 400 LOC/file, ≤ 15 files/dir |
| 18 | NFR-12 | `test_makefile_verify_system_target_exists` | verify_target | verify-system target present in Makefile |
| 19 | NFR-12 | `test_makefile_verify_system_exits_zero_and_passes` | verify_target | make verify-system exit 0 + "verify-system: PASS" |

---

## Cross-Cutting Test Cases

### NFR Integration (no single-FR owner; spans CLI surface)

| # | Test Function | Type | Derivation |
|---|---|---|---|
| 1 | `test_cli_module_entrypoint_status_exits_zero` | smoke | deployment (`python -m taskq_plus status` → exit 0;本專案為本機 CLI,無 HTTP endpoint,見 NP-01..03) |

### SEC verified_by (per SAD §6 — test names that double as security threat tests)

| # | Test Function | Owner FR | Threat | Type |
|---|---|---|---|---|
| 1 | `test_sec_t01_command_metacharacters_rejected_at_submit` | FR-01 | T-01 (tampering at TB-01) | security_grep |
| 2 | `test_sec_t02_dag_depth_cap_rejects_deep_chain` | FR-06 | T-02 (DoS at TB-01) | boundary |
| 3 | `test_sec_t03_executor_never_uses_shell_true` | FR-02 | T-03 (EoP at TB-02) | security_grep |
| 4 | `test_sec_t04_subprocess_timeout_marks_task_timeout` | FR-02 | T-04 (DoS at TB-02) | timeout |
| 5 | `test_sec_t05_plugin_path_or_url_spec_rejected` | FR-07 | T-05 (EoP at TB-03) | validation |
| 6 | `test_sec_t06_secrets_absent_from_audit_log_and_task_store` | FR-08 | T-06 (info disclosure at TB-04) | integration |
| 7 | `test_sec_t07_concurrent_writes_leave_task_store_parseable` | FR-02 | T-07 (tampering at TB-04) | integration |
| 8 | `test_sec_t08_audit_events_share_one_correlation_id` | FR-08 | T-08 (repudiation at TB-04) | integration |

---

## Summary

| Metric | Count |
|---|---|
| FRs covered | 8 / 8 (FR-01..FR-08) |
| NFRs covered (full table) | 5 / 12 (NFR-01, NFR-02, NFR-03, NFR-04, NFR-10) |
| NFRs covered (deferred) | 7 / 12 (NFR-05, 06, 07, 08, 09, 11, 12) |
| Total active test cases (FR + NFR full + SEC + cross-cutting) | 94 |
| Deferred test cases (out-of-scope for in-process predicates) | 19 |
| Grand total tracked in TEST_SPEC | 113 |
| By type: happy_path | 20 |
| By type: validation/failure | 17 |
| By type: boundary | 6 |
| By type: state_transition | 12 |
| By type: fault_injection | 0 (folded into integration/security_grep) |
| By type: integration | 20 |
| By type: security_grep | 7 |
| By type: timeout | 2 |
| By type: coverage_gate | 3 |
| By type: performance | 2 |
| By type: timing | 1 |
| By type: static_check | 1 |
| By type: security_gate | 1 |
| By type: meta_test | 1 |
| By type: smoke | 1 |
| Properties declared | 6 (FR-02 ×1, FR-03 ×2, FR-04 ×2, FR-06 ×1, FR-08 ×1) |
| Active NFR patterns applied | 8 (NP-04, NP-06, NP-07, NP-08, NP-09, NP-10, NP-13, NP-15) |

> The 94 active cases decompose as **64 FR + 21 NFR-active + 8 SEC verified_by + 1 cross-cutting**. The 19 deferred rows pull double-duty: they match TEST_INVENTORY.yaml's 64 FR + 40 NFR = 104 base count (64 + 21 + 19 = 104 ✓), and the 9-entry delta (8 SEC + 1 cross-cutting) is the TEST_SPEC-specific layer that TEST_INVENTORY.yaml does not enumerate (SAD §6 threat verified_by + deployment smoke live above the strict TEST_INVENTORY contract). Grand total tracked = 113 = 104 + 9.

---

*Generated by: derive_test_cases.md v1.1 | harness-methodology v2.13.0 (Round 2 of taskq-plus Phase 2)*