[mypy]
# `harness/` is a vendored submodule; its own mypy.ini governs its checks.
# Excluding it here prevents mypy from loading BOTH `./harness_cli.py` and
# `./harness/harness_cli.py` as the same module name when run from root.
exclude = ^harness/|^mutants/