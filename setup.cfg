[coverage:run]
# __main__.py is a dedicated entry-point module (supports `python -m taskq_plus`).
# The `if __name__ == "__main__":` block is unreachable from test code; exclude
# at the file level per the FR-05 coverage escape hatch (not a pragma target).
omit =
    03-development/src/taskq_plus/__main__.py
